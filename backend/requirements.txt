# --- LinkedIn Automation backend ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
sqlmodel>=0.0.22,<0.1
pydantic-settings>=2.6,<3.0

# Ingestion — LinkedIn via Apify
httpx>=0.27

# Scheduler (continuous, incremental re-check)
apscheduler>=3.10,<4.0

# Virality model + backtester + local TF-IDF + content-gap scorer
scikit-learn>=1.5,<2.0
numpy>=1.26
scipy>=1.11

# Market demand — Google Trends + search-suggest
pytrends>=4.9

# Brain Wiki — YAML frontmatter for markdown pages
pyyaml>=6.0

# LLM (Anthropic API fallback; default uses Claude Code CLI)
anthropic>=0.40,<1.0

# Postgres driver (cloud deployments only — SQLite needs no driver)
psycopg2-binary>=2.9
