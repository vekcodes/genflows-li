# Serverless (Vercel) requirements for api/index.py — the backend minus what can't or
# shouldn't run in a function: uvicorn (Vercel provides the server), faster-whisper and
# pytrends (both lazily imported, never installed here; Whisper is CPU-heavy and transcripts
# come from Apify in the cloud). Local dev uses backend/requirements.txt instead.
fastapi>=0.115,<1.0
sqlmodel>=0.0.22,<0.1
pydantic-settings>=2.6,<3.0
yt-dlp>=2024.12.13
youtube-transcript-api>=0.6.2
apscheduler>=3.10,<4.0
scikit-learn>=1.5,<2.0
numpy>=1.26
scipy>=1.11
httpx>=0.27
pyyaml>=6.0
anthropic>=0.40,<1.0
psycopg2-binary>=2.9
